# گزارش انتقال و بازبینی بک‌اند

مدل‌های `User`، `Product`، `Category`، `Coupon`، `Payment`، `Post` و `Comment` به Mongoose منتقل شده‌اند. APIها در `app/api` هستند و برای دسترسی‌های کاربری و مدیریت از access/refresh token داخل کوکی‌های `HttpOnly` استفاده می‌کنند.

## بهبودهای اعمال‌شده

- نام متغیر پایگاه‌داده یکپارچه شد: `MONGODB_URI`.
- OTP به‌صورت hash ذخیره می‌شود، ۹۰ ثانیه اعتبار دارد و پس از پنج خطا دیگر قابل استفاده نیست.
- فهرست محصولات، پست‌ها و منابع مدیریتی، متادیتای pagination شامل `page`، `limit`، `totalItems` و `totalPages` دارند.
- role guard برای همهٔ APIهای `/api/admin/*` اعمال شده است.
- در نسخهٔ رزومه‌ای، endpoint پرداخت مستقیماً پرداخت را `COMPLETED` ثبت می‌کند، موجودی را کم می‌کند و سبد خرید را خالی می‌سازد. در نسخهٔ production باید این بخش به callback تأییدشدهٔ درگاه منتقل شود.

## ایرادهای مهم بک‌اند قبلی

- `APP_DB` در کد با `MONGODB_URI` در env نمونه تفاوت داشت و برنامه را از اتصال بازمی‌داشت.
- دو مسیر و دو middleware متداخل برای auth وجود داشت؛ یکی از آن‌ها به ورود ایمیل/رمز وابسته بود، در حالی‌که جریان اصلی OTP است.
- routeهای post و comment در router اصلی mount نشده بودند.
- در سبد خرید از `req.user` به‌جای `req.user._id` استفاده شده بود.
- پرداخت بدون اتصال واقعی به درگاه، فوراً `COMPLETED` ثبت و موجودی را کم می‌کرد؛ این کار باعث سفارش تقلبی یا تکراری می‌شود.
- مدل `User` فیلد `likedProducts` را دوبار تعریف کرده و `otp.code` دارای typo در `default` بود.

## راه‌اندازی

۱. `.env.example` را به `.env.local` کپی و مقادیر MongoDB، دو secret JWT و کلید کاوه‌نگار را وارد کنید.

۲. برای تست OTP بدون ارسال پیامک، `OTP_TEST_MODE=true` قرار دهید. این متغیر فقط برای توسعه است و هرگز نباید در production فعال باشد.

۳. یک کاربر را مستقیماً در MongoDB با `role: "ADMIN"` ایجاد یا ارتقا دهید تا APIهای مدیریت فعال شوند.

۴. پیش از فعال کردن فروش واقعی، تکمیل مستقیم پرداخت را با endpoint تأیید پرداخت درگاه جایگزین کنید؛ فقط همان endpoint باید payment را `COMPLETED` کند و داخل transaction موجودی، کوپن و سفارش کاربر را به‌روزرسانی کند.
